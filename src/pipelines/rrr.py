"""
Rewrite-Retrieve-Read: rewrite query for clarity, then retrieve from global index, then generate answer.
Do not remove or rename this file.
"""

# TODO: Implement run(query, index, embedder, top_k, generator) -> (rewritten_query, retrieved_passages, answer).
# First rewrite/expand query (e.g. with LLM), then retrieval.retrieve(index, rewritten_query, top_k), then generate.
